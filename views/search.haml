- unless lemmas.nil?
  %ul
    - lemmas.each do |l|
      %p
        %li
          %b
            %a{:href => "/lemmas/#{l.id}"}= l.lemma
        - l.translations.each do |t|
          %li
            - if t.target
              = t.source
              = t.target
